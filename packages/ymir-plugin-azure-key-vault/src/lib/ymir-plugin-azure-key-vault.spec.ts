import { ymirPluginAzureKeyVault } from './ymir-plugin-azure-key-vault';

describe('ymirPluginAzureKeyVault', () => {
  it('should work', () => {
    expect(ymirPluginAzureKeyVault()).toEqual('ymir-plugin-azure-key-vault');
  });
});
